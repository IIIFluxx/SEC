gradlew run. 
Program takes in a filename to parse instead of Console Input.

Provide file named rand.txt for the Worksheet parse input.

Provide file named onestop.txt to test whether it only accepts two stops or more (i.e. it is EXPECTED that this throws an error).